# MongoDB Configuration
MONGODB_URI=mongodb://localhost:27017/quickride

# Alternative MongoDB configurations:
# For MongoDB Atlas (cloud):
# MONGODB_URI=mongodb+srv://<username>:<password>@cluster0.xxxxx.mongodb.net/quickride?retryWrites=true&w=majority

# For MongoDB with authentication:
# MONGODB_URI=mongodb://<username>:<password>@localhost:27017/quickride

# Server Configuration
PORT=8080

# Environment
NODE_ENV=development
